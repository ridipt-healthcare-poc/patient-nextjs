runtime: nodejs20
service: patient

instance_class: F2

automatic_scaling:
  min_instances: 1
  max_instances: 3

env_variables:
  NODE_ENV: production
  NEXT_PUBLIC_API_URL: "https://api.medsparsh.com"
